- defaults:
    link:
      create: true
- clean:
    ~/:
      force: true
    ~/.config:
      force: true
      recursive: true
- link:
    ~/.bash_profile: bash/bash_profile
    ~/.bashrc: bash/bashrc
    ~/.bash_logout: bash/bash_logout
    ~/.config/kitty/kitty.conf: kitty/kitty.conf
    ~/.config/sway/:
      glob: true
      path: sway/*
    ~/.config/waybar/config:
      path: waybar/config.json
    ~/.config/waybar/style.css:
      path: waybar/style.css
    ~/.local/share/rofi/themes/nord.rasi:
      path: rofi/nord.rasi
    ~/.config/electron-flags.conf:
      path: ./wayland/electron-flags.conf
    ~/.config/zathura/zathurac:
      path: /home/yubo/config/zathura/zathurarc
    ~/indentconfig.yaml:
      path: /home/yubo/config/latex/mysettings.yaml
    ~/.zshrc:
      path: /home/yubo/config/zsh/zshrc
    ~/.p10k.zsh:
      path: /home/yubo/config/zsh/p10k.zsh
    ~/.config/rofi/config.rasi:
      path: /home/yubo/config/rofi/config.rasi
- shell:
    - command: dst='/etc/environment'; sudo rm -f $dst; for env in $(fd '^env$'); do cat <(echo -e "\n# From $env") $env | sudo tee -a $dst; done
      description: "Environment File Generator"
    - command: sudo cp './virt/autostart/windows-virt.service' '/usr/lib/systemd/system/'
      description: "Windows Virtualization Service"
    - command: for pkg in $(fd pkgs.yaml); do sudo python ./plugin/package.py $pkg; done;
      stdout: true
      description: "Install pkgs.yaml in subfolders"
- package:
    - ide:
        - visual-studio-code-bin
        - jetbrains-toolbox
    - java:
        - openjdk-src
        - openjdk-doc
        - jdk-openjdk
    - rust-cli:
        - fd
        - ripgrep
    - decompresser:
        - ark
        - unrar
        - unarchiver
        - lzop
        - lrzip
    - notebook:
        - xmind
        - typora-free
    - common:
        - dolphin # File Explorer
        - gwenview # Image Viewer
        - ntfs-3g
    - discord
    - git
